<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme._26ApiGoogleMap" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="popupMenuBackground">@drawable/menu_background</item>
    </style>


    <style name="Theme._26ApiGoogleMap" parent="Base.Theme._26ApiGoogleMap" />
</resources>